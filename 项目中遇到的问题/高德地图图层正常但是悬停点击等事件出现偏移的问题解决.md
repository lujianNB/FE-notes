# 高德地图图层正常但是悬停点击等事件出现偏移的问题解决

## 问题描述

**在高德地图中进行涂层绘制时发现涂层渲染一切正常，但是在涂层上添加点击悬停等事件时，发现事件的触发位置发生偏移。**

## 问题原因

**主要是由于地图元素的外层dom使用了transform:scale的css样式**

## 解决方法

**尽量避免使用该样式，或者在外层缩小放大了多少倍，在地图的dom元素上反向放大缩小多少倍。例如transform:scale(1/3)和transform:scale(3)**